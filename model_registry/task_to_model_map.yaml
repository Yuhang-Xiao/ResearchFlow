version: 2026-05-07
tasks:
  binary_classification: {baseline_models: [dummy_majority, logistic_regression], model_families: [linear_model, tree_ensemble, gradient_boosting], not_applicable_models: [ordinary_least_squares_without_link]}
  multiclass_classification: {baseline_models: [dummy_majority, multinomial_logistic_regression], model_families: [linear_model, tree_ensemble, gradient_boosting], not_applicable_models: [binary_only_models]}
  imbalanced_classification: {baseline_models: [dummy_majority, class_weighted_logistic_regression], model_families: [linear_model, balanced_ensemble, gradient_boosting], not_applicable_models: [accuracy_only_selection]}
  regression: {baseline_models: [dummy_median, ridge_regression], model_families: [linear_model, tree_ensemble, gradient_boosting], not_applicable_models: [classification_models_without_reframing]}
  count_regression: {baseline_models: [dummy_median, seasonal_naive_when_time_ordered, poisson_glm], model_families: [generalized_linear_model, two_part_model, tree_ensemble, gradient_boosting, lag_feature_forecaster, sequence_model_candidate, automl_candidate], not_applicable_models: [uncensored_ols_for_sparse_counts_without_diagnostics]}
  zero_inflated_count: {baseline_models: [two_part_baseline, zero_rule_baseline, poisson_glm], model_families: [two_part_model, generalized_linear_model, tree_ensemble, gradient_boosting, hurdle_or_zero_inflated_model, lag_feature_forecaster, sequence_model_candidate, automl_candidate], not_applicable_models: [single_naive_regression_only]}
  extreme_event_prediction: {baseline_models: [quantile_baseline, threshold_binary_baseline, top_k_risk_baseline], model_families: [quantile_regression, tree_ensemble, gradient_boosting, conformal_or_interval_candidate, sequence_model_candidate], not_applicable_models: [mean_only_regression]}
  time_series_forecasting: {baseline_models: [seasonal_naive, rolling_mean, last_observation], model_families: [classical_forecasting, machine_learning_forecaster, gradient_boosting, sequence_model_candidate, automated_forecasting_candidate], not_applicable_models: [random_split_validation]}
  panel_or_grouped_prediction: {baseline_models: [group_mean_baseline, pooled_linear_model, leave_group_out_baseline], model_families: [mixed_effects, tree_ensemble, group_aware_ml, spatial_or_group_generalization, sequence_model_candidate], not_applicable_models: [group_leaking_random_split]}
  spatial_generalization: {baseline_models: [region_mean_baseline, pooled_linear_model], model_families: [spatial_features_ml, tree_ensemble, hierarchical_model], not_applicable_models: [random_split_only]}
  causal_inference: {baseline_models: [covariate_balance_table, regression_adjustment], model_families: [propensity_score, doubly_robust, causal_forest], not_applicable_models: [predictive_only_claims]}
  survival_analysis: {baseline_models: [kaplan_meier, cox_ph], model_families: [cox_model, survival_forest, parametric_survival], not_applicable_models: [plain_regression_on_censored_time]}
  reinforcement_learning: {baseline_models: [random_policy, heuristic_policy, historical_policy], model_families: [q_learning, dqn, constrained_rl], not_applicable_models: [unconfirmed_reward_training]}
  optimization: {baseline_models: [greedy_heuristic, status_quo], model_families: [linear_programming, mixed_integer_programming, heuristic_search], not_applicable_models: [unconstrained_ranking_only]}
  Bayesian_modeling: {baseline_models: [empirical_rate, beta_binomial], model_families: [bayesian_glm, hierarchical_bayes, bayesian_time_series], not_applicable_models: [point_estimate_only_when_uncertainty_required]}
  anomaly_detection: {baseline_models: [robust_zscore, isolation_forest], model_families: [density_model, isolation_forest, one_class_svm], not_applicable_models: [supervised_metrics_without_labels]}
  clustering: {baseline_models: [kmeans, hierarchical_clustering], model_families: [partition_clustering, density_clustering, mixture_model], not_applicable_models: [classification_accuracy_without_labels]}
  ranking_or_prioritization: {baseline_models: [score_sorting, risk_rule_baseline], model_families: [learning_to_rank, calibrated_risk_model, constrained_ranking], not_applicable_models: [unconstrained_best_accuracy_only]}
