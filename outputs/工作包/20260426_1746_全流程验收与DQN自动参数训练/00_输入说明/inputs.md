# 输入说明

用户要求执行“全流程真实验收 + DQN 自动参数合成与 GPU 训练”。

关键输入：项目状态、canonical feature tables、workflow recipes、model registry、references、Zotero 目录、myenv1 GPU 环境。

限制：不修改 data/01_raw，不写 Zotero 数据库，不把 experimental DQN 结果标记为 formal policy conclusion。
