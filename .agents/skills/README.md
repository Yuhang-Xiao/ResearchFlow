# Repo-scoped Skills

This directory contains the repo-scoped Codex skills for ResearchFlow OS.

The public repository uses `.agents/skills/` as the single skill directory. The former root-level skill mirror was removed during the GitHub release cleanup to avoid duplicate files.
