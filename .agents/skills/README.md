# Repo-scoped Skills

This directory contains repo-scoped skills for workflow1. The legacy `skills/` directory is kept for compatibility, and key workflow skills are mirrored in both places when added by Codex.

